{
  "name": "bus-ticket-booking",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [
    "monorepo",
    "react",
    "express",
    "husky",
    "lint-staged"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "lint-staged": {
    "server/**/*.js": ["cd server && npm run lint"],
    "client/**/*.{js,jsx,ts,tsx}": ["cd client && npm run lint"]
  }
}
